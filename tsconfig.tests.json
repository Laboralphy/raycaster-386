{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Tests run under Node: the differential harness reads the original
       engine off disk and bundles it. The library itself never sees these. */
    "types": ["node"]
  },
  "include": ["src", "tests", "demos", "vitest.bench.config.ts"]
}
